// src/components/Footer.jsx
import { Layout } from 'antd';

const { Footer } = Layout;

const FooterComponent = () => {
    return (
        <Footer style={{ textAlign: 'center', background: '#DCDCDC', color: 'black' }}>
            osTicket Task Dev Build v0.1
        </Footer>
    );
};

export default FooterComponent;
